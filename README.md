
# La-Phone

Теперь работаем через git, а не через Upload files.

Как вносить изменения: отредактировать нужный файл локально в этой папке →
`git add <файл>` → `git commit -m "описание правки"` → `git push`.
Живая версия (GitHub Pages) обновляется автоматически через 1-2 минуты после push.
